export interface CreateTravelPackageRequest {
    location: string
    name: string
    openDate: string
    endDate: string
}

export interface UpdateTravelPackageRequest {
    location: string
    name: string
    openDate: string
    endDate: string
}